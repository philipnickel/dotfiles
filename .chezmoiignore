# Ignore macOS-only applications on non-macOS systems
{{ if ne .chezmoi.os "darwin" }}
# macOS-specific GUI applications
.config/sketchybar/
.config/borders/
.aerospace.toml
{{ end }}

# Ignore Linux-only configurations on non-Linux systems
{{ if ne .chezmoi.os "linux" }}
# Linux-specific configs (if any)
{{ end }}
